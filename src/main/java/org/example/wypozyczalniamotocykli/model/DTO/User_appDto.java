/*
package org.example.wypozyczalniamotocykli.model.DTO;

import org.example.wypozyczalniamotocykli.model.User_app;
import org.example.wypozyczalniamotocykli.model.Role;
import lombok.*;
public record User_appDto(String username, String password, Role role) {
    public User_app toUserApp() {
        return User_app
                .builder()
                .username(username)
                .password(password)
                .role(role)
                .build();
    }
}
*/
